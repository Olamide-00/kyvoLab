# Base URL of the blog API. Leave empty to use the built-in starter posts in src/data/posts.ts.
# Expected endpoints: GET {base}/posts and GET {base}/posts/{slug}  (see src/lib/blog.ts)
VITE_BLOG_API_URL=
